import { NextPage } from 'next';
import Image from 'next/image';
import dayjs from 'dayjs';
import { PanButton } from 'components';

export const Button: NextPage = () => {
  return (
    <div>
      <PanButton>123</PanButton>
    </div>
  );
};
